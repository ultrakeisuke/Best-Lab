<% provide(:title, "プロフィール設定") %>

<%= render "users/shared/edit_profile_form", profile_form: @profile, profile: Profile.find(params[:id]), url: users_profile_path(current_user.profile), method: :patch %>
