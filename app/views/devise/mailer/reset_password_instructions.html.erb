<p><%= @resource.email %>さま</p>

<p>下記のリンクをクリックし、パスワードを再設定してください。</p>

<p><%= link_to 'こちらをクリックしてください。', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>このメールに覚えのない場合は、お手数ですがメールを破棄してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまで、パスワードは変更されません。</p>
